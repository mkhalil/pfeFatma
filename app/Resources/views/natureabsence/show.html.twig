{% extends 'base.html.twig' %}

{% block body %}
    <h1>Natureabsence</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ natureAbsence.id }}</td>
            </tr>
            <tr>
                <th>Libellenature</th>
                <td>{{ natureAbsence.libelleNature }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('natureabsence_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('natureabsence_edit', { 'id': natureAbsence.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
