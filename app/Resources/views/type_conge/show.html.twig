{% extends 'base.html.twig' %}

{% block body %}
    <h1>Type_conge</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_Conge.id }}</td>
            </tr>
            <tr>
                <th>Libelletype</th>
                <td>{{ type_Conge.libelleType }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('type_conge_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('type_conge_edit', { 'id': type_Conge.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
