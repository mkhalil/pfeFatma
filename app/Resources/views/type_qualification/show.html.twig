{% extends 'base.html.twig' %}

{% block body %}
    <h1>Type_qualification</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_Qualification.id }}</td>
            </tr>
            <tr>
                <th>Libelletypeq</th>
                <td>{{ type_Qualification.libelleTypeQ }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('type_qualification_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('type_qualification_edit', { 'id': type_Qualification.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
