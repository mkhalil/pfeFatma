{% extends 'base.html.twig' %}

{% block body %}
    <h1>Type_formation</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_Formation.id }}</td>
            </tr>
            <tr>
                <th>Libelletypef</th>
                <td>{{ type_Formation.libelleTypeF }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('type_formation_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('type_formation_edit', { 'id': type_Formation.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
